# API environment for the MASTER branch / production.
# Connected to postgres-prod on localhost:5433, database barbaros_pos_prod.
# This file IS committed to git on purpose — it only contains non-secret local config.

APP_ENV=production
DATABASE_URL=postgresql://barbaros:barbaros@localhost:5433/barbaros_pos_prod
PORT=3001
