# API environment for the DEVELOP branch / local development.
# Connected to postgres-dev on localhost:5432, database barbaros_pos_dev.
# This file IS committed to git on purpose — it only contains non-secret local config.

APP_ENV=develop
DATABASE_URL=postgresql://barbaros:barbaros@localhost:5432/barbaros_pos_dev
PORT=3000
